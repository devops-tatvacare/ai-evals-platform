# Database (overridden by Docker Compose — only needed for non-Docker development)
DATABASE_URL=postgresql+asyncpg://evals_user:evals_pass@localhost:5432/ai_evals_platform

# File storage
FILE_STORAGE_TYPE=local
FILE_STORAGE_PATH=./backend/uploads

# Server
API_PORT=8721
CORS_ORIGINS=http://localhost:5173

# LLM Providers (fill in your keys)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
DEFAULT_LLM_PROVIDER=gemini
EVAL_TEMPERATURE=0.1

# Kaira API (for live adversarial testing — optional)
KAIRA_API_URL=
KAIRA_AUTH_TOKEN=
KAIRA_TEST_USER_ID=
